# TODO
#1. Delete the neon serverless project
#1. If neon serverless cli is installed, uninstall it
#2. If neon serverless config dir exists (~/.config/neonctl), delete it
#3. If neon bindings cli is installed, uninstall it
#4. Don't do this by default, but prompt user for uninstalling NVM and Rust